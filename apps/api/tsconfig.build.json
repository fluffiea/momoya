{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "outDir": "dist",
    "rootDir": ".",
    "paths": {}
  },
  // 同时编译 src 和 scripts，让 seed.ts 等运维脚本也进入 dist
  // 编译产物结构：dist/src/index.js、dist/scripts/seed.js
  "include": ["src/**/*.ts", "scripts/**/*.ts"]
}
